#Aspirateur Roborock
  - platform: xiaomi_cloud_map_extractor
    host: !secret xiaomi_vacuum_ip
    name: Victor
    token: !secret xiaomi_vacuum_token
    username: !secret xiaomi_cloud_username
    password: !secret xiaomi_cloud_password
    country: "de"
    draw: ['all']
    attributes:
      - calibration_points
    colors:
      color_map_outside:  [17, 17, 17, 17]